<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.walletforest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".select_wallet_activity.SelectWalletActivity"></activity>
        <activity
            android:name=".select_category_activity.SelectCategoryActivity"
            android:launchMode="singleTask"></activity>
        <activity
            android:name=".transaction_detail_activity.TransactionDetailActivity"
            android:launchMode="singleTask" />
        <activity android:name=".main_activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>